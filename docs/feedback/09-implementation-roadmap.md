# 分阶段实施路线图与可执行任务清单

## 1. 文档目的

本路线图用于指导员工反馈与 360° 评价平台首期实施。它不是简单的功能列表，而是编码、数据库迁移、联调、测试和验收共同遵守的阶段门禁。

阶段编号表示依赖顺序，不表示自然周。没有确认人员投入前，不在本文件中承诺具体工期。

首期固定工程边界：

- `ruoyi-fastapi-backend`：唯一后端，新增核心业务模块 `module_feedback`。
- `ruoyi-fastapi-frontend`：系统管理端，只负责账号、组织、角色、菜单、字典和运维。
- `feedback-frontend`：评价业务平台，包含 HR 工作台和员工工作台。
- PostgreSQL：唯一正式业务数据库。

## 2. 实施原则

### 2.1 垂直切片优先

优先完成能够真实闭环的小功能：

```text
页面操作
→ 后端校验
→ PostgreSQL持久化
→ 重新进入恢复
→ 权限校验
→ 自动化测试
```

不能把静态页面、内存演示或 Mock 数据视为阶段完成。

### 2.2 阶段门禁

- 同一时间只推进一个主阶段，修复前序阶段缺陷除外。
- 当前阶段的退出条件全部满足后，才能进入下一阶段。
- 若业务规则发生变化，先更新产品、领域、数据库和验收文档，再修改代码。
- 每个阶段只创建当前任务实际需要的文件，不批量创建未来空目录和空类。

### 2.3 生成器使用边界

- RuoYi 代码生成器只用于简单表的 DO、VO、DAO、基础 CRUD、权限码和菜单 SQL 参考。
- 必须先预览或生成到临时目录，再逐文件选择性集成。
- 问卷设计器借鉴现有表单构建器的组件面板、拖拽画布、属性面板和组件注册思路，但重新实现业务数据结构和渲染器。
- 发布冻结、任务生成、逐人提交、幂等控制、计分和报告必须手写领域逻辑。

### 2.4 首期明确不做

- 问卷模板、模板库和复制模板创建。
- Excel 导入。
- 强制分布、去最高分和去最低分。
- 复杂图表和人才分析。
- 公开链接、二维码、小程序和无登录参评。
- 邮件、短信等外部通知。
- 自动推断上级、同级和下级关系。
- 项目自动完成和完成后重新打开。

## 3. 总体阶段依赖

```text
P0 决策收口与技术预检
 │
 ▼
P1 工程基线与登录权限
 │
 ▼
P2 领域骨架与PostgreSQL迁移
 │
 ▼
P3 项目草稿与单选题垂直切片
 │
 ▼
P4 完整问卷设计器与指标
 │
 ▼
P5 人员关系、发布冻结与任务生成
 │
 ▼
P6 员工逐人答题、暂存与提交
 │
 ▼
P7 回收进度与HR手动完成
 │
 ▼
P8 计分与基础报告
 │
 ▼
P9 安全、性能、迁移与首期验收
```

## 4. 通用完成标准

每个任务只有同时满足以下条件才能勾选完成：

- 实现位于约定工程和分层中，没有创建平行账号、组织或权限体系。
- 后端状态和正式分数由服务层及数据库约束保护，不能只依赖前端按钮状态。
- 数据库变更包含 Alembic 迁移、中文注释、必要约束和索引。
- API 包含鉴权、权限码、参数校验、错误语义和操作日志。
- 前端包含加载、禁用、防重复提交、失败提示和权限错误处理。
- 直接相关测试通过；不能运行的测试明确记录原因。
- 用户可见流程经过真实浏览器验证，不只依赖静态检查。
- 对应产品、数据、API或验收文档已经同步更新。

## 5. P0：决策收口与技术预检

### 5.1 目标

消除会影响数据库和计分实现的关键不确定性，并验证 PostgreSQL、代码生成器和现有 RuoYi 公共能力可以作为实施基础。

### 5.2 任务清单

- [x] `P0-01` 确认关系权重规则。
  - 输出：是否预设默认权重、是否允许项目内调整、权重总和校验规则。
  - 验证：已同步更新[决策记录](./08-decisions-and-open-questions.md)和[计分基线](./03-domain-model-and-state-machine.md)。
- [x] `P0-02` 确认 HR 原始答案可见范围。
  - 输出：按角色和数据范围定义“进度可见、答案可见、报告可见”。
  - 验证：已在[架构与工程边界](./02-architecture-and-boundaries.md)形成权限矩阵；HR基础角色默认没有原始答案权限。
- [x] `P0-03` 确认缺失关系和未完成任务计算规则。
  - 输出：是否允许部分关系为空，以及完成项目后的权重重新归一化规则。
  - 验证：[领域模型与状态机](./03-domain-model-and-state-machine.md)已区分“不适用”和“缺失关系”，并提供4个手工计分样例。
- [x] `P0-04` 确认首期报告导出范围。
  - 输出：仅网页表格，或额外提供简单 Excel 导出；Excel 导入仍不在首期。
  - 验证：已确认首期仅网页表格、不提供文件导出，并更新[MVP范围与场景F](./06-mvp-scope-and-acceptance.md)。
- [x] `P0-05` 核对后端 PostgreSQL 和 Redis 开发配置。
  - 输出：开发环境变量说明、独立开发数据库和测试数据库约定。
  - 验证：已创建并验证`ruoyi_feedback_dev`、`ruoyi_feedback_test`及Redis逻辑库2/3隔离；两个数据库均完成RuoYi基线初始化并处于Alembic唯一head，项目自带`app doctor`全部通过，详见5.4节。
- [x] `P0-06` 验证 RuoYi PostgreSQL 代码生成链路。
  - 输出：用一张临时简单表完成导入、预览和生成，记录需要保留及删除的生成内容。
  - 验证：临时表完成导入、9类模板预览和ZIP导出，生成物只进入`build/p0-codegen`；临时表、元数据和ZIP均已清理，详见[代码生成器使用说明](./07-code-generator-usage.md)。
- [x] `P0-07` 固化首期权限码和角色映射。
  - 输出：HR、员工及兼具两种身份用户的权限矩阵。
  - 验证：权限码和角色映射已固化在[架构与工程边界](./02-architecture-and-boundaries.md)，双身份使用权限并集。

### 5.3 退出条件

- 影响数据模型和计分的待确认事项已经明确，或书面接受当前推荐默认值。
- PostgreSQL、Redis、Alembic、登录和代码生成器完成真实环境预检。
- 没有为了赶进度而把关键业务规则留成代码中的临时常量。

### 5.4 2026-09-02执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 开发配置脱敏核对 | 读取现有配置对象，只输出类型、主机、端口、库名和是否配置密码 | 默认数据源`primary`为PostgreSQL，`127.0.0.1:5432/ruoyi-fastapi`；Redis为`127.0.0.1:6379`逻辑库2；后端端口9099 |
| 独立开发/测试数据库初始化 | `python scripts\feedback_p0_database_precheck.py --env=dev --create --yes` | 创建并初始化`ruoyi_feedback_dev`、`ruoyi_feedback_test`；`sys_user`、`sys_dept`、`list_table`、`list_column`均存在 |
| 幂等复核 | `python scripts\feedback_p0_database_precheck.py --env=dev` | 两个库均未重新创建、未重新执行基线SQL，数据库隔离和Redis键空间隔离继续通过 |
| Alembic基线 | `ruoyi db heads/history`及两个独立库的`upgrade/current/history` | 仓库只有一个head`20260902_01_feedback_baseline`，两个库当前revision与head一致 |
| PostgreSQL、Redis、传输加密 | 预检脚本分别以开发库+Redis 2、测试库+Redis 3运行项目自带`app doctor` | 两组数据库、Redis和传输加密检查均通过 |
| 基础登录接口 | 真实调用`/login`、`/getInfo`、`/logout`，不记录Token | Bearer Token签发、用户信息读取和退出均成功 |
| PostgreSQL元数据视图 | `ruoyi gen db-list --env=dev --output=json --table-name=sys_user` | 能识别`sys_user`、`sys_user_role`、`sys_user_post` |
| 代码生成器 | 临时表导入、生成配置、`gen preview`、`gen export` | 预览9类模板；导出10,144字节ZIP；未写入正式源码；清理后物理表和生成元数据查询均为0 |
| Compose配置 | 两个PostgreSQL compose文件执行`docker compose ... config --quiet` | 开发compose固定`ruoyi_feedback_dev`，测试compose固定`ruoyi_feedback_test`和Redis逻辑库3，解析通过 |
| 直接相关测试 | `python -m pytest`运行P0脚本、数据库CLI、Alembic补全、生成器运行时和CLI契约测试 | 52项通过，最终复核耗时43.58秒 |

#### 预检发现与处理

| 发现 | 证据 | 处理结论 |
|---|---|---|
| 初始仓库没有Alembic revision | 初次`db current`报`alembic_version`不存在，`heads/history`均为0 | 已建立空操作基线revision，并在两个独立库真实升级验证；后续`fb_`迁移以该revision为父版本 |
| 初始开发/测试数据库未按反馈项目隔离 | 初始开发配置指向`ruoyi-fastapi`，测试compose使用同名逻辑库 | 已实际创建两个独立库并固化compose覆盖；主机模式预检使用进程环境覆盖，不改写或泄露`.env.dev`凭据 |
| `gen create-table`不是原子流程 | 无表注释的临时表在物理建表提交后导入失败，命令错误文本为空并留下物理表 | 作为已知非阻塞边界保留；正式表只允许Alembic创建并补齐注释，再由生成器导入 |
| 生成骨架依赖管理端公共能力 | 预览Vue代码包含`v-hasPermi`、`right-toolbar`、`pagination`和全局下载 | 生成前端不集成到`feedback-frontend`；只选择性参考字段映射、权限和基础DAO |

#### P0阶段结论

```text
阶段：P0 决策收口与技术预检
完成日期：2026-09-02
实现范围：P0-01至P0-07全部完成
数据库迁移：已建立`20260902_01_feedback_baseline`；尚未创建评价业务迁移
验证命令与结果：见本节“已验证”和“预检发现与处理”
浏览器验收：P0不包含业务页面；基础登录使用真实HTTP接口验证，未执行业务浏览器验收
遗留问题：`gen create-table`非原子、生成Vue依赖管理端公共能力，均已通过固定使用边界规避
是否允许进入下一阶段：是
```

## 6. P1：工程基线与登录权限

### 6.1 目标

建立可独立运行的 `feedback-frontend`，并打通现有 RuoYi 登录、当前用户和权限链路。

### 6.2 任务清单

- [x] `P1-01` 初始化 `feedback-frontend` 工程。
  - 输出：Vue 3、Vite、Element Plus、Pinia、Vue Router、Axios 和基础构建配置。
  - 验证：开发启动、生产构建和基础页面加载成功。
- [x] `P1-02` 建立平台端公共请求层。
  - 输出：Token注入、传输加密、响应解密、401处理、统一错误和下载能力。
  - 验证：契约与管理端及后端当前实现一致，不直接导入管理端源码。
- [x] `P1-03` 接入登录、退出和当前用户信息。
  - 输出：`/login`、`/getInfo`、`/logout` 对应 API、Store 和页面。
  - 验证：刷新后恢复登录状态，失效Token回到登录页。
- [x] `P1-04` 建立 HR 与员工双工作台布局。
  - 输出：固定业务路由、工作台切换和无权限页面。
  - 验证：不同权限用户只能看到对应入口，后端仍是最终权限边界。
- [x] `P1-05` 建立前端测试基线。
  - 输出：组件、Store/API 测试配置和真实浏览器端到端测试骨架。
  - 验证：至少一个组件测试、一个 Store 测试和一个登录后路由测试通过。
- [x] `P1-06` 注册后端 `module_feedback` 空模块入口。
  - 输出：模块路由、权限依赖和健康探针式只读接口，不创建业务空壳类。
  - 验证：登录用户和未登录用户访问结果符合预期。

### 6.3 预计主要文件

```text
feedback-frontend/package.json
feedback-frontend/vite.config.js
feedback-frontend/src/main.js
feedback-frontend/src/router/index.js
feedback-frontend/src/stores/auth.js
feedback-frontend/src/stores/permission.js
feedback-frontend/src/utils/request.js
feedback-frontend/src/utils/transportCrypto.js
feedback-frontend/src/layouts/HrLayout.vue
feedback-frontend/src/layouts/EmployeeLayout.vue
ruoyi-fastapi-backend/module_feedback/__init__.py
ruoyi-fastapi-backend/module_feedback/controller/feedback_controller.py
```

### 6.4 退出条件

- `feedback-frontend` 可独立开发和构建。
- 真实账号登录、刷新恢复、退出、权限隔离和双工作台切换通过验证。
- 平台端没有复制管理端的用户、部门和角色页面。

### 6.5 2026-09-02执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 前端依赖与工程配置 | `npm install`及锁文件核对 | Vue 3、Vite、Element Plus、Pinia、Vue Router、Axios、Vitest和Playwright依赖安装成功 |
| 前端单元测试 | `npm test` | 组件、Store和API共3个测试文件、7项测试通过 |
| 前端生产构建 | `npm run build` | Vite生产构建通过 |
| 浏览器E2E | `npm run test:e2e` | 登录后默认路由、双工作台切换和员工权限隔离共3项通过 |
| 后端模块测试 | `python -m pytest tests\module_feedback\controller\test_feedback_controller.py` | 模块注册、鉴权依赖和只读响应共4项通过 |
| 后端格式与静态检查 | `ruff check`、`ruff format --check` | `module_feedback`及其直接测试检查通过 |
| 请求层兼容性 | 单元测试、源码契约核对和强制加密真实链路 | Token、401、统一错误、下载、策略同步、请求加密和响应解密均已建立；未跨目录导入管理端源码 |
| 真实后端模块入口 | 使用现有9099后端调用`/feedback/health` | 未登录返回业务码401；真实登录后返回业务码200；退出后的Token再次访问返回业务码401 |
| 真实浏览器登录链路 | 本机真实后端、PostgreSQL、Redis和Chromium | 管理员登录、进入HR工作台、切换员工工作台、刷新恢复当前用户、退出回到登录页均通过 |
| 强制传输加密 | 独立临时后端启用`TRANSPORT_CRYPTO_MODE=required` | 明文`/login`被拒绝；平台端通过加密信封完成登录，加密`/getInfo`刷新恢复成功，并可正常退出 |

#### 实现边界

- `feedback-frontend` 是独立工程，没有直接导入 `ruoyi-fastapi-frontend/src`。
- 登录页使用 `ElForm`、`ElFormItem` 和显式点击/回车处理器，没有使用原生表单提交主链路。
- 当前工作台只包含工程和权限基线占位页，没有复制管理端的用户、部门、岗位或角色页面。
- `module_feedback` 当前只注册受 `PreAuthDependency` 保护的只读健康入口；尚未创建项目、问卷、任务等业务空壳类。
- P1 不改变数据库结构，没有新增评价业务 Alembic 迁移。

#### P1阶段结论

```text
阶段：P1 工程基线与登录权限
完成日期：2026-09-02
实现范围：P1-01至P1-06全部完成
数据库迁移：无；P1未创建评价业务表
验证命令与结果：前端7项单元测试、3项浏览器E2E、生产构建，后端4项接口测试及Ruff检查全部通过
浏览器验收：真实账号完成登录、双工作台切换、刷新恢复、权限隔离和退出；强制传输加密模式下重复验证通过
遗留问题：无P1阶段阻塞项；评价业务页面、领域表和业务接口按P2及后续阶段实现
是否允许进入下一阶段：是
```

## 7. P2：领域骨架与 PostgreSQL 迁移

### 7.1 目标

建立首期核心表、枚举、实体和数据库约束，为后续垂直业务提供可靠持久化基础。

### 7.2 任务清单

- [x] `P2-01` 最终确认首期 ER 模型和字段字典。
  - 输出：表、字段、类型、可空性、默认值、外键、唯一约束和中文注释。
  - 验证：与领域状态机、发布快照和计分输入逐项对应。
- [x] `P2-02` 创建项目与问卷配置迁移。
  - 输出：`fb_project`、问卷版本、页面、题目、选项、指标和题目绑定表。
  - 验证：空库升级和降级通过，关键权重及顺序约束生效。
- [x] `P2-03` 创建关系与参与人员迁移。
  - 输出：评价关系、项目被评价人和评价任务表。
  - 验证：项目、评价人、被评价人和关系唯一约束生效。
- [x] `P2-04` 创建答卷与结果迁移。
  - 输出：答卷、答案和计分结果表。
  - 验证：答卷题目唯一、正式分数使用 `NUMERIC`、提交数据不可软删除。
- [x] `P2-05` 实现状态和题型枚举。
  - 输出：项目状态、任务状态、题型、答案类型和结果类型枚举。
  - 验证：API、数据库和前端使用同一语义，不散落魔法字符串。
- [x] `P2-06` 实现 SQLAlchemy DO 和基础 DAO。
  - 输出：实体映射和最小查询方法；可选择性借鉴代码生成结果。
  - 验证：关系加载、事务边界和异步会话行为有直接测试。
- [x] `P2-07` 建立状态转换测试。
  - 输出：准备、发布、完成及待评价、暂存、提交、关闭未完成的合法转换测试。
  - 验证：非法逆向转换在服务层被拒绝。

### 7.3 退出条件

- 全部首期表可以在独立 PostgreSQL 空库中由 Alembic 创建。
- 关键唯一约束、检查约束和索引有自动化验证。
- 实体模型能够表达发布冻结、逐人任务、提交快照和计分依据。

### 7.4 2026-09-02执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| ER模型和字段字典 | [P2领域表与字段字典](./10-p2-field-dictionary.md)与迁移、模型逐项核对 | 13张表覆盖项目、冻结问卷、关系、人员快照、任务、答卷、答案和计分依据；字段类型、可空性、默认值、外键和业务约束已固化 |
| Alembic迁移链 | `python -m alembic -c alembic.ini heads/history --verbose` | 仓库唯一head为`20260902_02_feedback_domain`，父revision为P0基线 |
| 独立开发/测试库升级 | `python scripts\feedback_p0_database_precheck.py --env=dev` | `ruoyi_feedback_dev`和`ruoyi_feedback_test`均升级到P2 head；数据库、Redis隔离和传输加密诊断继续通过 |
| 可逆迁移 | `python scripts\feedback_p2_schema_verify.py --database ruoyi_feedback_test --migration-cycle --yes` | 脚本确认13张评价表为空后降到P0基线，评价表数量变为0，再升级回P2 head并恢复13张表 |
| PostgreSQL结构核验 | `feedback_p2_schema_verify.py`分别检查开发库和测试库 | 13张表、182个字段/中文注释、11个`NUMERIC(12,4)`字段、10个关键约束、9个关键索引均通过；182个字段与SQLAlchemy模型的类型和可空性一致 |
| 不可变和正式分数边界 | 数据库元数据与模型测试 | `fb_answer_sheet`、`fb_answer`、`fb_score_result`均无软删除字段；评价表不存在`REAL/DOUBLE PRECISION`正式分数字段 |
| 异步DAO与数据库约束 | 显式启用隔离PostgreSQL的DAO测试 | 真实异步Session完成项目/版本/页面/任务关系加载；DAO不提交事务；重复任务、重复单题答案和非法任务状态被数据库拒绝；外层事务回滚后不留业务数据 |
| 后端自动化测试 | `RUN_FEEDBACK_POSTGRES_TESTS=1 python -m pytest tests\module_feedback` | P1模块入口及P2枚举、状态机、实体、DAO、迁移门禁共42项通过 |
| 前端枚举契约 | `npm test` | 前端状态、题型、答案值、关系和结果类型与后端枚举、数据库检查约束一致；前端共9项测试通过 |
| 格式与静态检查 | `ruff check`、`ruff format --check`、`git diff --check` | P2后端代码、迁移、脚本和测试检查通过 |

#### 实现边界

- P2只建立领域持久化、枚举、状态门禁和最小DAO，不提前实现P3项目CRUD、问卷编辑API或页面。
- 发布、提交和完成的完整事务编排仍在后续服务阶段实现；P2 DAO只负责访问和锁定，不自行`commit`。
- `fb_project_target`和`fb_assignment`保存发布时姓名、部门快照，同时继续引用RuoYi `sys_user/sys_dept`。
- `fb_score_result.score`允许为空表达“数据不足”，并保存原配置权重、实际权重、完成数量和`calculation_basis`，不使用0掩盖缺失关系。
- 全仓库`alembic check`仍会报告P2范围外既有AI插件表和RuoYi基线索引/默认值差异；P2使用限定`fb_`表的182字段、约束、索引和真实迁移循环完成无漂移核验，未修改这些既有差异。

#### P2阶段结论

```text
阶段：P2 领域骨架与PostgreSQL迁移
完成日期：2026-09-02
实现范围：P2-01至P2-07全部完成
数据库迁移：20260902_02_feedback_domain；13张fb_表，支持降到P0基线并重新升级
验证命令与结果：后端42项、前端9项测试通过；两个隔离库结构核验通过；测试库降级/升级循环通过
浏览器验收：P2不新增用户可见流程；沿用P1浏览器基线，本阶段不以静态页面冒充业务功能
遗留问题：全仓库Alembic自动比较存在P2范围外既有漂移；已明确隔离，P2的fb_模型与数据库182字段一致
是否允许进入下一阶段：是
```

## 8. P3：项目草稿与单选题垂直切片

### 8.1 目标

先用一种题型验证专用问卷编辑器的核心架构，不在模型尚未验证时同时开发五种题型。

### 8.2 任务清单

- [x] `P3-01` 实现准备阶段项目 CRUD。
  - 输出：项目列表、创建、编辑和删除 API 及 HR 页面。
  - 验证：只有准备阶段允许编辑和删除，数据范围生效。
- [x] `P3-02` 定义问卷草稿 API 契约。
  - 输出：页面、题目、选项的读取和保存 DTO，包含版本号或锁版本。
  - 验证：字段使用业务语义，不暴露 `tag`、`vModel` 等 UI 生成器结构。
- [x] `P3-03` 建立题型注册表和共享渲染器。
  - 输出：题型配置、默认值、编辑器组件、答题组件、只读组件和验证函数注册方式。
  - 验证：未知题型明确失败，不静默降级成文本框。
- [x] `P3-04` 实现问卷编辑器三栏骨架。
  - 输出：问卷大纲、题型面板、题目画布和属性面板。
  - 验证：借鉴现有构建器交互，但不直接导入管理端源码。
- [x] `P3-05` 实现单选题完整配置。
  - 输出：标题、说明、必答、选项、分值、填写原因、复制、删除和排序。
  - 验证：选项具有稳定业务标识，排序后不会错配已有答案。
- [x] `P3-06` 实现草稿保存和恢复。
  - 输出：显式保存、加载态、防重复保存和并发版本冲突提示。
  - 验证：保存后刷新或重新登录能够恢复同一草稿。
- [x] `P3-07` 实现电脑与手机预览。
  - 输出：预览共用题型注册表和渲染器。
  - 验证：预览不写正式答案，窄屏布局无横向溢出。
- [x] `P3-08` 完成单选题垂直切片测试。
  - 输出：后端保存测试、前端编辑测试和真实浏览器保存恢复测试。
  - 验证：编辑、保存、重载和预览形成完整闭环。

### 8.3 退出条件

- 单选题不是静态演示，已经真实持久化到 PostgreSQL。
- 编辑器、预览器和后续答题器可以共享题型协议。
- 草稿并发冲突不会静默覆盖其他修改。

### 8.4 2026-09-02执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 准备阶段项目CRUD | 隔离后端、`feedback-frontend`与`ruoyi_feedback_test`真实浏览器验收 | 使用真实`admin`登录后完成项目创建、列表回显、进入编辑器和页面删除；删除后列表归零 |
| 问卷草稿持久化 | 真实浏览器保存后整页刷新，并由后端重新读取 | 问卷说明、题目、3个选项、`5.0000/3.5000/1.2500`分值及“需改进要求说明”规则均从PostgreSQL无损恢复 |
| 状态、数据范围和并发门禁 | `RUN_FEEDBACK_POSTGRES_TESTS=1 python -m pytest tests\module_feedback` | 真实异步Session覆盖数据范围拒绝、非准备阶段拒绝、过期锁版本拒绝、软删除后不可读取；后端P1-P3共50项通过 |
| 单选题编辑能力 | Pinia单元测试及Playwright端到端测试 | 标题、说明、必答、计分、批量选项、4位小数分值、附加原因、复制、删除和上下排序通过；复制时题目和选项生成全新稳定业务标识 |
| 共享题型协议与预览 | `QuestionRenderer`组件测试和真实浏览器电脑/手机预览 | 编辑器与预览器共用题型注册表和渲染器；未知题型明确失败；手机预览按条件显示附加原因输入 |
| 预览只读边界 | 真实浏览器操作前后查询`fb_answer_sheet`和`fb_answer` | 当前验收项目的答卷和答案记录均为0；预览只维护页面临时输入，不调用正式答案保存接口 |
| 前端自动化测试 | `npm test`、`npm run test:e2e` | 7个测试文件共17项单元/组件测试通过；4项浏览器测试通过，包含编辑、复制、排序、删除、保存、重载和手机预览 |
| 构建与静态检查 | `npm run build`、`python -m ruff check module_feedback tests\module_feedback` | 前端生产构建通过；P3后端代码与测试Ruff检查通过；构建仅有第三方纯注释和大chunk提示 |

#### 实现边界

- P3只开放单页单选题垂直切片；多页、其余题型、指标绑定和跨页排序按P4实施，不用本阶段原型冒充完整问卷设计器。
- 项目、问卷版本、页面、题目和选项继续复用P2领域表，本阶段无新增Alembic迁移；所有正式分值继续使用PostgreSQL `NUMERIC`。
- 草稿保存由后端事务整体替换未冻结页面结构，并同时校验项目状态、草稿状态、数据范围和锁版本；DAO不自行提交。
- 前端不使用本地Mock或浏览器存储作为正式数据源；Pinia只保存当前编辑会话，刷新后必须重新调用草稿API。
- P3预览不创建评价任务、答卷或答案；真实员工答题和逐人提交仍属于P5-P6范围。

#### P3阶段结论

```text
阶段：P3 项目草稿与单选题垂直切片
完成日期：2026-09-02
实现范围：P3-01至P3-08全部完成
数据库迁移：无新增迁移；复用P2的fb_project、fb_questionnaire_version、fb_questionnaire_page、fb_question和fb_question_option
验证命令与结果：后端50项、前端17项、浏览器4项测试通过；前端生产构建和后端Ruff检查通过
浏览器验收：真实登录 -> 创建项目 -> 配置单选题 -> 保存PostgreSQL -> 整页刷新恢复 -> 手机预览 -> 删除项目闭环通过
遗留问题：生产构建主chunk超过500kB，属于后续按路由/组件拆包优化项，不阻塞P3业务正确性
是否允许进入下一阶段：是
```

## 9. P4：完整问卷设计器与指标

### 9.1 目标

在已验证的题型注册架构上增加其余题型、多页问卷、排序和指标配置。

### 9.2 技术预检与执行状态

2026-09-02已完成[P4完整问卷设计器与指标技术预检](./11-p4-questionnaire-designer-precheck.md)，并按预检协议完成P4-01至P4-09。P4只交付准备阶段问卷与指标草稿，不包含发布、任务生成或员工答题。

实现必须先完成以下基础改造：

- 新增`description_doc`和`page_code`的Alembic增量迁移，不新增平行问卷表。
- 将草稿DTO升级为五题型判别联合，保持一个GET/PUT聚合接口和服务层单事务保存。
- 使用稳定业务Code关联页面、题目、选项和指标，保存响应整体水合数据库ID。
- 扩展共享题型注册表并建立四位定点数工具，再逐题型完成编辑、预览、只读和测试。
- 富文本采用受限Tiptap JSON；P4返回后端权威的发布完整性问题，P5再以指标权重精确`100.0000`、所有计分题均已绑定作为发布硬门禁。

### 9.3 任务清单

- [x] `P4-01` 实现星级评分题。
  - 输出：最低分、最高分、必答和只读展示。
  - 验证：未作答与零分语义不混淆。
- [x] `P4-02` 实现数字输入评分题。
  - 输出：最低分、最高分、小数位数和默认值。
  - 验证：前后端使用相同边界和精度规则。
- [x] `P4-03` 实现滑动评分题。
  - 输出：最低分、最高分、步长和默认值。
  - 验证：键盘与移动端触控可用。
- [x] `P4-04` 实现问答题。
  - 输出：必答、字数限制和只读展示。
  - 验证：问答题可以绑定指标，但不参与数值总分。
- [x] `P4-05` 实现多页、问卷大纲和页面排序。
  - 输出：新增、重命名、排序和删除页面，题目跨页移动。
  - 验证：展示题号根据页面和题目顺序动态计算。
- [x] `P4-06` 实现题目复制、删除和排序完整行为。
  - 输出：稳定的 `clientKey`、业务Code、数据库ID和保存映射规则。
  - 验证：复制后不会复用选项ID，删除引用项时有明确校验。
- [x] `P4-07` 实现指标维护和题目绑定。
  - 输出：指标名称、权重、顺序和题目绑定。
  - 验证：一道计分题首期最多属于一个指标，权重合计为100%。
- [x] `P4-08` 实现原始满分预览和编辑完整性校验。
  - 输出：前端实时预览和后端权威校验。
  - 验证：问答题不计分，前端结果不能替代后端结果。
- [x] `P4-09` 完成五种题型测试矩阵。
  - 输出：编辑、预览、校验、序列化和反序列化测试。
  - 验证：每种题型至少覆盖有效值、边界值、缺失值和只读状态。

### 9.4 退出条件

- 五种题型、多页、复制、删除、排序、预览和指标绑定全部使用同一业务协议。
- 问卷保存后可无损恢复，前后端完整性校验一致。
- 编辑器没有混入模板库、任意表单代码导出等首期外能力。

### 9.5 2026-09-02执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| P4迁移与结构契约 | `python scripts\feedback_p2_schema_verify.py --database ruoyi_feedback_test --designer-migration-cycle --yes` | 带既有页面完成P3→P4→P3→P4；两次均确定性回填同一`pageCode`；13张表、184个字段中文注释、184项模型字段契约、11个`NUMERIC(12,4)`字段及关键约束/索引通过 |
| 后端协议与真实持久化 | `RUN_FEEDBACK_POSTGRES_TESTS=1 python -m pytest tests\module_feedback` | 63项全部通过；覆盖五题型判别联合、受限富文本、多页、指标、绑定、乐观锁和新Session恢复 |
| 后端静态门禁 | `python -m ruff check ...`、`python -m ruff format --check ...` | 45个Python文件检查通过 |
| 前端组件与Store | `npm test` | 9个测试文件共32项通过；覆盖五题型注册表、共享渲染、四位定点数、多页/跨页/复制和保存后ID水合 |
| 前端生产构建 | `npm run build` | 构建通过；Tiptap仅进入懒加载的问卷编辑器chunk；保留第三方PURE注释和主chunk大于500kB两个非阻塞警告 |
| 自动化浏览器回归 | `npm run test:e2e` | 4项通过；P4用例覆盖五题型、两页、指标、保存重载和手机预览 |
| 真实前后端浏览器闭环 | 隔离后端9100、前端5177、`ruoyi_feedback_test`和真实`admin`登录 | 创建两页五题型问卷，保存受限富文本、1个指标和4条计分题绑定；整页刷新无损恢复；滑动题键盘操作有效；手机预览无横向溢出 |
| 预览只读边界 | 浏览器操作后直接查询PostgreSQL | 验收项目包含2页、5种题型、1个指标、4条绑定，`fb_answer_sheet`和`fb_answer`均为0 |

#### 实现边界

- P4继续复用P2领域表、RuoYi认证/权限/数据范围和P3单一草稿GET/PUT接口；没有建立平行用户、问卷或登录体系。
- 草稿保存由服务层在一个事务中完成结构校验、页面/题目/选项/指标/绑定整体替换和数据库ID水合；DAO不提交事务。
- 草稿结构错误阻止保存；权重不足或计分题未绑定等发布问题可随草稿保存并由后端返回。P5发布时必须再次使用同一权威校验器。
- 富文本只接受受限Tiptap JSON并由同一schema只读渲染；没有开放任意HTML、链接、图片或表格。
- 实现未生成评价任务、答卷或答案，未进入P5/P6范围。

#### P4阶段结论

```text
阶段：P4 完整问卷设计器与指标
完成日期：2026-09-02
实现范围：P4-01至P4-09全部完成
数据库迁移：新增20260902_03_feedback_designer；P3→P4→P3→P4有数据往返通过
验证命令与结果：后端真实PostgreSQL 63项、前端32项、浏览器4项通过；生产构建、Ruff检查和格式检查通过
浏览器验收：真实登录 -> 创建项目 -> 配置两页五题型/富文本/指标 -> 保存PostgreSQL -> 整页刷新恢复 -> 键盘滑动 -> 电脑/手机预览 -> 删除项目闭环通过
遗留问题：主chunk仍超过500kB且VueUse存在第三方PURE注释警告；不影响P4正确性，后续独立做性能拆包
是否允许进入下一阶段：是
```

## 10. P5：人员关系、发布冻结与任务生成

### 10.1 目标

完成从可编辑项目到不可变进行中项目的事务化发布，并生成员工评价任务。

### 10.2 决策收口与技术预检记录

```text
完成日期：2026-09-02
基线提交：da9b53c feat: 完成评价平台P4完整问卷设计器与指标
预检范围：准备期人员/关系/评价人选择持久化、RuoYi数据范围、默认关系、自评派生、聚合API、组合校验、事务锁、幂等、只读和测试门禁
关键结论：新增长期领域表fb_evaluator_selection；fb_assignment只在发布事务内生成
产品边界：未改变首期范围；员工待办正式API和页面仍属于P6
详细协议：docs/feedback/12-p5-publication-precheck.md
是否允许进入P5实现：是，从P5-00开始
```

### 10.3 任务清单

- [x] `P5-00` 增加发布配置迁移和实体。
  - 输出：`20260902_04_feedback_publication`、`fb_evaluator_selection`、既有草稿固定关系回填和新项目默认关系初始化。
  - 验证：P4→P5→P4→P5有数据往返、固定关系不重复、开发库/测试库唯一head、14张表和196个模型/中文注释字段契约一致。
- [x] `P5-01` 实现被评价人选择和展示快照。
  - 输出：项目内候选人员接口，复用`sys_user`、`sys_dept`和RuoYi数据范围，保存发布时姓名和部门历史快照。
  - 验证：越权或失效人员不能保存/发布；配置后、发布前的改名调岗进入最终快照，发布后的变化不改变历史语义。
- [x] `P5-02` 实现评价关系配置。
  - 输出：上级、同级、下级、自己、其他及自定义关系、启停和权重。
  - 验证：首期只有“自己”自动绑定，其他关系由HR手工配置。
- [x] `P5-03` 实现评价人双栏选择和任务预览。
  - 输出：按被评价人和关系选择评价人，持久化到`fb_evaluator_selection`，由后端派生自评并返回权威任务预览。
  - 验证：刷新可恢复；准备期不存在正式任务；不能形成评价人、被评价人、关系完全重复的选择或任务。
- [x] `P5-04` 组合并补齐发布完整性校验器。
  - 输出：复用P4问卷与指标问题清单，补齐人员、关系和任务配置错误清单。
  - 验证：一次返回可操作的问题集合，不只返回第一个模糊错误。
- [x] `P5-05` 实现事务化发布。
  - 输出：冻结问卷版本、人员快照、计分配置并生成任务。
  - 验证：任一步失败全部回滚，不出现“已发布但任务不完整”。
- [x] `P5-06` 实现发布幂等和并发控制。
  - 输出：锁版本、项目状态条件更新和任务唯一约束处理。
  - 验证：两个并发请求只有一个执行写入；相同项目/版本的后到请求返回同一结果且不重复任务。
- [x] `P5-07` 实现发布后二次确认和只读页面。
  - 输出：不可逆提示、冻结配置只读查看和权限处理。
  - 验证：不能通过直接API绕过前端进入编辑状态。
- [x] `P5-08` 完成发布链路端到端测试。
  - 输出：HR创建、配置、发布和发布后只读的真实浏览器测试，联合真实PostgreSQL核验员工任务生成。
  - 验证：任务数量与配置预览一致且可按评价人查询；不以员工占位页面冒充P6待办页面已完成。

### 10.4 P5-00执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 隔离库升级和应用诊断 | `python scripts\feedback_p0_database_precheck.py` | `ruoyi_feedback_dev`和`ruoyi_feedback_test`均升级到唯一head `20260902_04_feedback_publication`；PostgreSQL、Redis隔离和传输加密诊断通过，原配置库未改变 |
| P5有数据迁移往返 | `python scripts\feedback_p2_schema_verify.py --database ruoyi_feedback_test --publication-migration-cycle --yes` | P4→P5→P4→P5通过；既有页面和目标保留，选择表降级移除并重建，五类固定关系没有重复，测试夹具已清理 |
| 开发库结构契约 | `python scripts\feedback_p2_schema_verify.py --database ruoyi_feedback_dev` | 14张表、196个中文注释字段、196项模型字段契约、12项关键约束和11项关键索引通过 |
| 后端真实持久化 | `RUN_FEEDBACK_POSTGRES_TESTS=1 python -m pytest -q tests\module_feedback` | 67项全部通过；覆盖新项目固定关系、自评选择持久化、选择业务唯一键和准备期不生成正式任务 |
| 后端静态门禁 | `python -m ruff check ...`、`python -m ruff format --check ...` | 49个Python文件检查通过 |

#### 实现边界

- P5-00只交付`fb_evaluator_selection`、五类固定关系协议、既有草稿回填和新项目默认关系初始化。
- 准备期仍不向`fb_assignment`写入任何正式任务；被评价人选择、评价人聚合保存、自评派生、任务预览和事务化发布尚未实现。
- 本小步没有前端页面或浏览器流程，因此不以P4页面或静态原型充当P5验收证据。

#### P5-00结论

```text
小步：P5-00 发布配置迁移和实体
完成日期：2026-09-02
实现范围：评价人选择配置表、复合外键与业务唯一键、固定关系回填、新项目默认关系初始化
数据库迁移：新增20260902_04_feedback_publication；P4→P5→P4→P5有数据往返通过
验证命令与结果：真实PostgreSQL 67项、两个隔离库结构与应用诊断、Ruff检查和格式检查通过
浏览器验收：无；P5-00不含用户可见流程
遗留问题：P5-01至P5-08尚未实现，当前不能发布项目或生成正式任务
是否允许进入下一小步：是，进入P5-01
```

### 10.5 P5-01至P5-08执行与验收记录

#### 已验证

| 检查项 | 命令或方式 | 结果 |
|---|---|---|
| 聚合配置和权限 | 后端Service/API测试、前端API/Store测试 | 候选人分页与数据范围、目标/关系/评价人整体保存、自评派生、稳定问题码、409冲突和双权限分治通过 |
| 发布事务和快照 | `RUN_FEEDBACK_POSTGRES_TESTS=1 python -m pytest -q tests\module_feedback` | 78项全部通过；发布时刷新人员快照，冻结问卷和计分规则，生成唯一`PENDING`任务；发布后改名不改变历史快照 |
| 幂等、并发与回滚 | 真实PostgreSQL双`AsyncSession`和故障注入测试 | 重试返回`alreadyPublished=true`且不增加任务；并发只有一次写入；中途失败时项目、版本、快照和任务整体回滚 |
| 数据库迁移与结构 | `python scripts\feedback_p2_schema_verify.py --database ruoyi_feedback_test --publication-migration-cycle --yes` | P4→P5→P4→P5往返通过；14张表、196个中文注释/模型字段、11个`NUMERIC`字段、12项约束和11项索引一致 |
| 前端自动化与构建 | `pnpm test`、`pnpm build` | 12个文件共41项通过；包含四位小数、分页、脏状态、权威问题展示、冻结组件、后端问题回填和防重复发布；生产构建通过 |
| 浏览器回归 | `pnpm run test:e2e` | 7项通过；P5覆盖聚合配置、保存恢复、二次确认、发布只读及“仅人员管理/仅发布”权限组合 |
| 真实前后端与PostgreSQL闭环 | 隔离后端9100、前端5177、`ruoyi_feedback_test`和真实`admin`登录 | 通过UI创建项目和P4合法问卷/指标，配置1名目标、2名评价人和2项任务，刷新恢复后发布；数据库核对`ACTIVE`、`FROZEN`、计分快照、2条选择和2条唯一`PENDING`任务，新标签重载为冻结只读且无控制台错误/警告 |
| 静态门禁 | `python -m ruff check ...`、`python -m ruff format --check ...` | 55个Python文件检查和格式通过 |

#### 实现边界

- P5复用RuoYi的登录、用户、部门、角色、权限和数据范围；没有建立平行员工或登录体系。
- 准备期保存到`fb_evaluator_selection`，正式任务只在后端单事务发布时物化；前端预览和请求都不能指定正式任务或计分快照。
- P5完成后项目处于进行阶段，问卷和人员配置只读；员工待办、答题、暂存和提交仍属于P6，本阶段不以员工占位页冒充完成。
- 生产构建仍有第三方VueUse PURE注释和主chunk大于500kB的非阻塞警告，不影响P5正确性，后续独立拆包。

#### P5阶段结论

```text
阶段：P5 人员关系、发布冻结与任务生成
完成日期：2026-09-02
实现范围：P5-00至P5-08全部完成
数据库迁移：20260902_04_feedback_publication；P4→P5→P4→P5有数据往返通过
验证命令与结果：后端真实PostgreSQL 78项、前端41项、浏览器7项通过；生产构建、Ruff检查和格式通过
浏览器验收：真实登录 -> 创建项目/P4问卷 -> 配置人员关系 -> 保存PostgreSQL -> 整页刷新恢复 -> 权威预览 -> 二次确认发布 -> 冻结只读闭环通过，预览与正式任务均为2
遗留问题：员工待办和逐人答题属于P6；前端大chunk为已知非阻塞优化项
是否允许进入下一阶段：是，进入P6-01
```

### 10.6 退出条件

- 发布是单事务或具有等价原子性的完整操作。
- 发布后配置不可原地覆盖，所有任务可追溯到冻结版本。
- 后端可以按评价人从真实任务数据查询待办，不使用前端模拟数据；员工待办正式API和页面按P6实施。

## 11. P6：员工逐人答题、暂存与提交

### 11.1 目标

完成员工从“我的待办”按被评价人逐人暂存和逐人提交的核心业务流程。

### 11.2 任务清单

2026-09-02：P6-01至P6-09已实现并通过本阶段门禁。完整协议与直接验证证据见[P6决策、预检与验收记录](./13-p6-answering-precheck.md)。

- [x] `P6-01` 实现我的待办和项目详情 API。
  - 输出：仅返回当前登录评价人的项目、被评价人和任务进度。
  - 验证：修改请求参数不能读取其他评价人的任务。
- [x] `P6-02` 实现员工待办页面。
  - 输出：项目进度、被评价人列表、任务状态和继续评价入口。
  - 验证：桌面和手机浏览器均可使用。
- [x] `P6-03` 实现分页答题器。
  - 输出：五种题型、附加原因、上一页、下一页和答题进度。
  - 验证：共用题型注册表，不重复实现另一套计分语义。
- [x] `P6-04` 实现草稿暂存。
  - 输出：允许答案不完整，保存答卷锁版本和最近保存时间。
  - 验证：重新进入恢复到正确被评价人、页面和答案。
- [x] `P6-05` 实现提交前校验与二次确认。
  - 输出：必答、范围、字数、选项原因和项目状态校验。
  - 验证：错误定位到具体页面和题目。
- [x] `P6-06` 实现事务化正式提交。
  - 输出：锁定答卷、保存答案和原始分、记录提交时间。
  - 验证：提交后不可修改，失败时不能留下半提交状态。
- [x] `P6-07` 实现提交幂等和并发防护。
  - 输出：幂等请求处理、唯一约束和状态条件更新。
  - 验证：重复点击和两个并发请求只形成一份正式答卷。
- [x] `P6-08` 实现“我评价的”只读历史。
  - 输出：已提交项目、被评价人和只读答案。
  - 验证：只能读取本人提交记录，且不能从只读页面恢复编辑。
- [x] `P6-09` 完成员工逐人提交端到端测试。
  - 输出：一名员工评价两名被评价人，先后独立提交。
  - 验证：提交第一人后第二人仍可填写，进度分别更新。

### 11.3 退出条件

- 员工可以真实完成暂存、恢复、校验、提交和只读历史闭环。
- 正式答案和分数由后端校验并持久化。
- 越权读取、越权提交、重复提交和完成后提交均被阻止。

### 11.4 执行与验收记录

| 范围 | 验证与结果 |
|---|---|
| 本人待办、归属和权限 | 7项员工API复用登录、当前用户和独立权限；HTTP测试拒绝注入评价人查询，真实PostgreSQL拒绝跨用户任务/答案访问 |
| 五题型与分页表单 | 真实Element Plus组件共用注册表；68项前端单测通过，覆盖必答/原因、零值、精度、分页定位、恢复、只读、确认、防重入和退出清空 |
| 草稿与正式提交 | 评价模块120项后端测试通过，包含真实PostgreSQL锁版本冲突、幂等并发、草稿/提交竞争、失败回滚、不可修改、不同关系独立和关闭后拒绝写入 |
| 共享设施回归 | 与本次日志修正直接相关的49项测试通过；合计后端169项通过 |
| 真实两人端到端 | `npm run test:e2e:live`通过：完整RuoYi登录→HR发布→员工2份待办→不完整暂存→刷新恢复→错误定位→逐人提交→只读历史→退出保护；无Mock业务API |
| 数据库逐条对账 | 2份他评各5条答案、原始总分均`53.7500`；自评任务仍待评价。大分值实际提交总分`200000006.9998`精确保留 |
| 迁移与结构 | 开发库/测试库升级到`20260902_06_feedback_permissions`；14表/196字段及中文注释/10项NUMERIC(12,4)+1项NUMERIC(18,4)/12关键约束/11关键索引通过；数据保值与权限安全降级测试通过 |
| 浏览器与构建回归 | 既有Mock接口E2E 7项通过；390×844长姓名/长题目/长选项无横向溢出；生产构建、Ruff及75个Python文件格式检查通过 |

可重复命令、测试文件与完整边界见[P6验收证据](./13-p6-answering-precheck.md#8-2026-09-02验收证据)。后端测试使用项目独立`.venv`和`ruoyi_feedback_test`；临时凭据与测试项目/账号/角色已清理。验收时未执行提交、推送或部署。

```text
阶段：P6 员工逐人答题、暂存与提交
完成日期：2026-09-02
实现范围：P6-01至P6-09全部完成
数据库迁移：20260902_05_feedback_answering、20260902_06_feedback_permissions
验证结果：评价后端120项+共享日志49项、前端68项、既有E2E 7项、真实业务E2E 1项全部通过
遗留边界：P7实际HR完成API/页面、P8正式汇总报告与P9上线门禁尚未实现；前端大chunk和第三方注释警告为非阻塞项
是否允许进入下一阶段：是，可进入P7决策预检与P7-01回收统计
```

## 12. P7：回收进度与 HR 手动完成

### 12.1 目标

让 HR 准确掌握回收情况，并以不可逆操作手动结束项目。

### 12.2 任务清单

- [ ] `P7-01` 实现回收统计查询。
  - 输出：应完成、已提交、已暂存、未开始、已关闭未完成和完成率。
  - 验证：统计口径与任务状态逐条对账。
- [ ] `P7-02` 实现进度明细和筛选。
  - 输出：按评价人、被评价人、关系和完成状态筛选。
  - 验证：数据范围和答案可见权限分别控制。
- [ ] `P7-03` 实现站内进度查看入口。
  - 输出：不依赖邮件、短信或外部链接。
  - 验证：普通员工不能进入HR进度页面。
- [ ] `P7-04` 实现完成项目预检。
  - 输出：提交、暂存和未开始数量，缺失关系和报告影响提示。
  - 验证：二次确认展示的是请求时最新统计。
- [ ] `P7-05` 实现事务化手动完成。
  - 输出：项目变为已完成，未提交任务变为已关闭未完成，记录完成人、时间和原因。
  - 验证：状态更新失败整体回滚。
- [ ] `P7-06` 实现完成并发控制。
  - 输出：完成操作和员工提交之间的锁定顺序及冲突结果。
  - 验证：不能出现项目已完成后仍新增正式提交。
- [ ] `P7-07` 完成有未交任务时结束的端到端测试。
  - 输出：同时包含已提交、已暂存和未开始任务的完成场景。
  - 验证：关闭状态、员工页面和后续报告输入一致。

### 12.3 退出条件

- 回收统计可以从任务明细复算，不能依赖不透明缓存计数。
- 只有有权限的 HR 可以完成项目。
- 项目完成后所有写入型答题接口均拒绝继续操作。

## 13. P8：计分与基础报告

### 13.1 目标

实现可追溯、可复算的百分制计分和首期表格型报告。

### 13.2 任务清单

- [ ] `P8-01` 固化计分测试样例。
  - 输出：单答卷、多评价人、多关系、仅自评和缺失关系样例及期望结果。
  - 验证：业务方能够手工复算。
- [ ] `P8-02` 实现题目原始分和指标百分制计算。
  - 输出：使用 `Decimal/NUMERIC` 的纯计算器和单元测试。
  - 验证：不使用二进制浮点数保存正式结果。
- [ ] `P8-03` 实现关系平均分和关系权重合成。
  - 输出：多人关系平均、自评对照和关系综合分。
  - 验证：无答卷关系不被错误当成零分。
- [ ] `P8-04` 实现缺失关系权重归一化。
  - 输出：实际完成关系重新归一化和缺失标记。
  - 验证：报告同时展示完成率、缺失关系和参与计算的权重。
- [ ] `P8-05` 实现个人总分和计算依据保存。
  - 输出：指标得分、关系得分、个人总分和输入版本快照。
  - 验证：结果可追溯到答卷集合和计算规则版本。
- [ ] `P8-06` 实现团队排名和团队明细。
  - 输出：表格排名、总分、指标分和自评/他评字段。
  - 验证：排序相同分数和缺失数据有稳定规则。
- [ ] `P8-07` 实现个人基础报告。
  - 输出：指标表、题目明细和自评/他评对比；首期仅授权HR查看。
  - 验证：不包含复杂图表和未确认的人才标签。
- [ ] `P8-08` 实现报告权限和原始答案可见策略。
  - 输出：报告、汇总和原始答案的独立权限检查。
  - 验证：有报告权限不自动等于可以查看评价人身份和全部原文。
- [-] `P8-09` 按P0决策实现或排除简单导出。
  - 输出：首期仅提供系统内网页表格，不实现Excel、CSV或其他报告文件导出。
  - 验证：取消依据见[决策记录2.4](./08-decisions-and-open-questions.md)；验收时确认无导出按钮和导出接口。
- [ ] `P8-10` 完成报告对账测试。
  - 输出：API结果、数据库结果和手工样例三方一致性测试。
  - 验证：舍入只发生在约定展示层，计算中间过程至少保留四位小数。

### 13.3 退出条件

- 所有正式得分由后端计算，且可用固定样例复算。
- 报告展示完成率和缺失数据，不用零分掩盖缺失评价。
- 首期报告保持表格化，没有提前引入复杂可视化体系。

## 14. P9：安全、性能、迁移与首期验收

### 14.1 目标

在功能闭环后完成跨模块安全检查、真实迁移、性能基线和首期发布验收。

### 14.2 任务清单

- [ ] `P9-01` 完成接口权限和数据范围审计。
  - 输出：所有 `/feedback` 接口的登录、权限码、资源归属和数据范围矩阵。
  - 验证：普通员工、HR、跨部门HR和管理员场景覆盖。
- [ ] `P9-02` 完成越权和输入篡改测试。
  - 输出：替换项目ID、任务ID、评价人ID、被评价人ID和答卷ID的负向测试。
  - 验证：不能依赖前端隐藏字段或按钮保护资源。
- [ ] `P9-03` 完成状态机和并发专项测试。
  - 输出：发布、提交、完成的重复请求和并发请求测试。
  - 验证：数据库最终状态唯一且可解释。
- [ ] `P9-04` 完成 PostgreSQL 迁移验证。
  - 输出：独立空库升级、已有RuoYi基线库升级和回滚策略记录。
  - 验证：不在正式数据上试验破坏性迁移。
- [ ] `P9-05` 检查查询计划和必要索引。
  - 输出：待办、进度、报告核心查询的 `EXPLAIN` 记录和索引调整。
  - 验证：不通过无依据的冗余索引掩盖查询设计问题。
- [ ] `P9-06` 完成审计和可观测性检查。
  - 输出：发布、提交、完成和报告计算的操作日志、错误日志和请求追踪。
  - 验证：日志不泄露完整答案、Token或敏感个人数据。
- [ ] `P9-07` 运行首期完整自动化验证。
  - 输出：后端测试、前端测试、构建、格式检查和关键E2E报告。
  - 验证：失败项不得以“与本次无关”直接忽略，必须分类和记录。
- [ ] `P9-08` 执行首期业务验收场景。
  - 输出：`06-mvp-scope-and-acceptance.md` 中全部场景的证据记录。
  - 验证：使用真实 PostgreSQL、真实后端和真实浏览器，不使用 Mock 数据替代正式验收。
- [ ] `P9-09` 完成交付文档。
  - 输出：部署配置、迁移步骤、回滚步骤、权限初始化、已知限制和运维说明。
  - 验证：明确区分“开发完成、验收通过、已部署、可正式使用”四种状态。

### 14.3 退出条件

- 首期核心流程、权限、并发、迁移和报告计分全部通过验收。
- 所有首期外能力保持未实现或明确隔离，没有半成品入口。
- 只有在部署和运行验证完成后，才能声明项目已可正式使用。

## 15. 跨阶段测试矩阵

| 测试层级 | 最低覆盖范围 | 首次建立阶段 | 持续执行阶段 |
|---|---|---|---|
| 后端单元测试 | 状态转换、发布校验、计分计算 | P2 | P2-P9 |
| 后端服务/API测试 | 权限、事务、幂等、数据范围 | P2 | P2-P9 |
| PostgreSQL迁移测试 | 空库升级、约束、索引 | P2 | P2-P9 |
| 前端组件测试 | 五种题型、编辑器、只读展示 | P3 | P3-P9 |
| 前端Store/API测试 | 登录、草稿、提交、错误处理 | P1 | P1-P9 |
| 浏览器E2E | 发布、逐人提交、手动完成、报告 | P3 | P3-P9 |
| 安全负向测试 | 越权、参数篡改、重复请求 | P5 | P5-P9 |
| 计分对账测试 | 样例、数据库、API、页面一致 | P8 | P8-P9 |

## 16. 首期里程碑

| 里程碑 | 对应阶段 | 可交付结果 |
|---|---|---|
| M1 平台可登录 | P0-P1 | 独立平台端、真实账号、双工作台和测试基线 |
| M2 问卷可保存 | P2-P3 | 项目草稿、单选题编辑、保存、恢复和预览 |
| M3 问卷可发布 | P4-P5 | 五种题型、指标、人员关系、冻结版本和真实评价任务生成 |
| M4 员工可评价 | P6 | 逐人暂存、逐人提交和只读历史 |
| M5 项目可完成 | P7 | 回收进度和HR手动完成 |
| M6 报告可复算 | P8 | 百分制计分和基础表格报告 |
| M7 首期可验收 | P9 | 权限、并发、迁移、性能和完整流程证据 |

## 17. 任务状态维护规则

- `[ ]`：未开始。
- `[~]`：进行中；开始任务时应补充负责人或执行任务链接。
- `[x]`：已完成；必须附验证命令、测试结果或验收证据。
- `[!]`：阻塞；必须记录阻塞原因、影响和解除条件。
- `[-]`：经决策取消；必须链接到决策记录，不能直接删除任务。

每完成一个阶段，应在本文件追加阶段验收记录：

```text
阶段：
完成日期：
实现范围：
数据库迁移：
验证命令与结果：
浏览器验收：
遗留问题：
是否允许进入下一阶段：是 / 否
```

## 18. 当前执行起点

`P0`至`P6`已经完成并通过各自阶段门禁。P6真实员工逐人暂存、恢复、提交、只读历史、权限/并发和PostgreSQL持久化闭环已验收。下一阶段为P7回收进度与HR手动完成；先明确回收统计口径、完成预检和项目排他锁协议，再从`P7-01`开始。正式汇总计分/报告仍由P8交付，P6完成不代表整个平台已上线就绪。
