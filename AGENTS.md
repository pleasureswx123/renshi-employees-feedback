# 项目协作规则

## 语言与沟通

- 默认使用中文回复。
- 项目文档、业务说明、提交说明和代码注释以中文为主；代码标识符使用英文。
- 所有结论必须区分：已验证、设计约定、推荐方案、待确认事项。
- 不得把页面原型、静态设计或局部测试描述为已经完成的正式功能。

## 项目定位

本仓库以 RuoYi-Vue3-FastAPI 为基础，实现员工反馈与 360° 评价平台。

三个主要工程的职责固定如下：

- `ruoyi-fastapi-backend`：唯一业务后端，复用现有认证、用户、部门、岗位、角色、权限、日志、数据库和缓存能力，并新增核心业务模块 `module_feedback`。
- `ruoyi-fastapi-frontend`：系统管理端，负责用户、部门、岗位、角色、菜单、字典、参数和运维管理，不承载评价业务页面。
- `feedback-frontend`：评价业务平台，内部按权限提供 HR 工作台和员工工作台。

不得在 `feedback-frontend` 或 `module_feedback` 中重复建设用户、员工、部门、角色或登录体系。

## 技术栈约束

### 后端

- 使用现有 FastAPI、SQLAlchemy 异步会话、Pydantic、Redis、JWT/OAuth2、Alembic 和 RuoYi 公共基础设施。
- 评价业务作为核心模块实现，不作为可卸载插件实现。
- 后端业务模块遵循现有 `controller / service / dao / entity` 分层。
- 接口统一使用 `/feedback` 前缀，并使用现有 `PreAuthDependency`、接口权限、当前用户和数据范围能力。
- 业务状态变更必须在服务层完成，并在需要时使用数据库事务和并发控制。

### 前端

- `feedback-frontend` 参考 `ruoyi-fastapi-frontend` 的 Vue 3、Vite、Element Plus、Pinia、Vue Router、Axios、Token、请求拦截、传输加密和权限实现。
- 不得跨目录直接导入 `ruoyi-fastapi-frontend/src` 源码；需要复用的能力应在平台端形成独立且兼容的实现。
- 业务表单必须使用 `ElForm`、`ElFormItem` 和 Element Plus 校验。
- 普通业务提交不得使用原生 `submit`、`@submit.prevent` 或 `native-type="submit"` 作为主链路；使用明确的点击或回车处理器，并处理加载、禁用、防重复提交和错误反馈。
- 不引入与 Element Plus 重叠的第二套 UI 框架。

## 数据库约束

- 项目数据库固定使用 PostgreSQL。
- 评价业务表使用 `fb_` 前缀，小写下划线命名。
- 主键默认使用 `BIGINT` 自增；分数和权重使用 `NUMERIC`，禁止使用浮点数保存正式得分。
- 复用 `sys_user.user_id`、`sys_dept.dept_id`、角色和岗位数据，不创建平行员工表。
- 配置类数据采用 RuoYi 审计字段风格；已提交答卷、答案和计算依据不得软删除或覆盖。
- 表和字段必须包含中文注释；关键业务关系必须有唯一约束、检查约束和必要索引。
- 数据库结构变更通过 Alembic 迁移交付，不只修改初始化 SQL。

## 核心业务不变量

- 项目状态为：准备阶段、进行阶段、已完成。
- 项目只由 HR 手动完成，不自动结束。
- 发布时冻结问卷、题目、指标、关系和计分配置。
- 一份评价任务的业务唯一键是：项目、评价人、被评价人、评价关系。
- 员工按被评价人逐人暂存、逐人提交。
- 已提交答卷不可修改；原始答案永久保留。
- HR 完成项目后，未提交任务变为“已关闭未完成”，不能继续答题。
- 报告只使用已提交答卷，并必须展示完成率和数据缺失情况。
- 正式计分由后端负责，前端计算只能用于编辑预览。

## 首期范围

首期不实现：

- 系统或企业问卷模板。
- Excel 导入。
- 强制分布。
- 去最高分、去最低分。
- 复杂报告图表。
- 公开链接、二维码、小程序或无登录参评。
- 邮件、短信外部通知。
- 自动推断上级、同级和下级关系。
- 项目自动完成和完成后重新打开。

首期仅允许已登录用户通过 `feedback-frontend` 的“我的待办”参评。

## 代码生成器使用规则

- RuoYi 代码生成器仅用于简单表的 CRUD 骨架、字段映射和权限代码参考。
- 生成代码必须先预览或生成到临时目录，经过逐文件检查后再集成。
- 不得让生成器覆盖现有文件或替代项目发布、版本冻结、任务生成、提交锁定、计分和报告等领域逻辑。
- 生成的 Vue 页面依赖 RuoYi 管理端公共组件，不能未经适配直接放入 `feedback-frontend`。

## 修改与验证

- 开始实现前先阅读 `docs/feedback/README.md` 及其指向的相关文档。
- 如果实现改变产品边界、状态机、数据模型、计分规则或首期范围，必须同步更新对应文档。
- 优先编写直接覆盖当前改动的后端测试、前端单元测试和关键流程端到端测试。
- 不能运行的验证项必须说明原因，不得将静态检查替代真实运行验证。
- 保留用户已有改动；不要处理与当前任务无关的文件。
- 未经明确要求，不提交、不推送、不部署。
